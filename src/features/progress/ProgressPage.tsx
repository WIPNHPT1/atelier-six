import { PageHeader } from '../../app/layout/PageHeader';
import { copy } from '../../content/copy.en-GB';

export default function ProgressPage() {
  return <PageHeader title={copy.progressScreen.title} />;
}
