---
description: Show build status cheaply
---
Without reading anything else: read `PROGRESS.md`, count ticked vs total steps, and list any BLOCKED or DEPLOY_PENDING lines. Show `Status`, `Step` and `Next action` from `RESUME.md`. Run `git log --oneline -5`. Reply in ≤6 lines.
